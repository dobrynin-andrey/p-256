options:
  max-time: 60
pipelines:
  default:
  - step:
      name: Deploy
      script:
      - apt-get update
      - apt-get -qq install sshpass
      - echo "Start deploy..."
      - ls -l
      - sshpass -p $SSH_PASS ssh -p $SSH_PORT $SSH_USERNAME@$SSH_HOSTNAME 'bash -s' < script.sh
      - echo "Done!"
definitions:
  caches:
    sshpass: ~/.sshpass/cache
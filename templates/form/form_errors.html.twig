{% block form_errors %}
    {% spaceless %}
        {% if errors|length > 0 %}
            <div class="alert alert-danger">
                <div>{{ (errors | first).message }}</div>
            </div>
        {% endif %}
    {% endspaceless %}
{% endblock form_errors %}